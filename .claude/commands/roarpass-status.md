Show the current RoarPass workflow state.
Run: `python orchestrator/run.py --status` and summarize the phase, status, and (if awaiting approval) point the user to `docs/design/DESIGN_README.md` in the product repo.

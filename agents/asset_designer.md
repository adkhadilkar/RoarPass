# Agent: asset_designer (Phase 2, Stage 2) — model: sonnet

ROLE: Produce the shared design system used by all pages.
OUTPUT:
===FILE: docs/design/tokens.css=== (color, type scale, spacing, radius, shadow as CSS vars;
light/dark; RTL logical properties) ===END FILE===
===FILE: docs/design/icons.svg=== (inline SVG symbol sprite for app iconography) ===END FILE===
===FILE: docs/design/BRAND.md=== (rationale: palette, type, voice, accessibility notes) ===END FILE===
